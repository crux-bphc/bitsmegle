# Local MongoDB started by `npm run db:up` (docker-compose.dev.yml).
# The database name in the path matters: src/db/mongo.ts calls client.db() with
# no argument, so the name is taken from this URI.
DB_URI=mongodb://127.0.0.1:27017/bitsmegle
SECRET_CLIENT_ID=
SECRET_CLIENT_SECRET=
REDIRECT_URI=http://localhost:5173
PUBLIC_BACKEND_URI=http://localhost:3000
PUBLIC_BACKEND_WS_URI=ws://localhost:3000
SECRET_GMAIL_USER=
SECRET_GMAIL_PASS=
BACKEND_PORT=3001
